# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments
.venv/
venv/
env/
ENV/
env.bak/
venv.bak/

# IDE
.vscode/
.idea/
.claude/
*.swp
*.swo
*~

# Testing
.pytest_cache/
.coverage
htmlcov/
.tox/
.nox/
*.cover  # Coverage annotation files

# Docker
docker-compose.override.yml

# Test results (TRACKED - convenience for research repo)
# test_results/*
# !test_results/.gitkeep

# Cache
!embeddings_cache/.gitkeep

# Logs
logs/*
!logs/.gitkeep
*_test_*.log

# Models (commit benchmark weights - they're research deliverables)
# Only ignore scratch/experimental models

# Data - track processed CSVs (research deliverables), ignore raw sources
# Specific ignore patterns below (lines 105+) handle raw data/Excel files

# Outputs
!outputs/.gitkeep

# Test config
test_config.yaml

# Jupyter Notebook
.ipynb_checkpoints

# Documentation (MkDocs generated site)
site/

# Environment variables
.env
.env.local
.env.*.local

# OS
.DS_Store
Thumbs.db

# Project specific (root level only)
/logs/
/outputs/
.gradio/

# Literature PDFs and markdown conversions (for personal reference only)
literature/

# Reference repositories (for development reference only)
reference_repos/

# Scratch documentation (brainstorming, temporary notes)
docs_burner/

# Raw training datasets (large files, not for version control)
train_datasets/boughter_raw/
train_datasets/boughter/raw/
train_datasets/ginkgo/

# Raw data in consolidated data/ directory (Phase 2+)
data/train/*/raw/

# Raw data files (download from paper supplementary materials)
data/test/*.xlsx
data/test/*.xls

# Source data files (Excel) - do not track
data/test/**/*.xlsx

# Coverage reports
coverage.xml

# Ruff cache
.ruff_cache/

# Experimental results and submissions (Ginkgo competition)
experiment_results/
ginkgo_submissions*/
external_datasets/

# Experimental models (Ginkgo competition)

# Ginkgo competition datasets (large, local only)
data/test/ginkgo/
train_datasets/ginkgo/

# Tessier dataset (large, local only)
train_datasets/tessier/

# Phase 5 Reorganization: Consolidated experiments/ structure
experiments/runs/*
experiments/checkpoints/*
!experiments/checkpoints/esm1v/
!experiments/checkpoints/esm2_650m/
!experiments/checkpoints/biophysical/
experiments/cache/*

# Keep directory structure
!experiments/runs/.gitkeep
!experiments/checkpoints/.gitkeep
!experiments/cache/.gitkeep
!experiments/runs/README.md
!experiments/checkpoints/README.md
!experiments/cache/README.md

# Benchmarks are versioned
!experiments/benchmarks/

# Coverage reports (JSON format)
coverage.json

# Exception: Commit canonical trained models (research artifacts, <50KB)
!experiments/checkpoints/esm1v/logreg/boughter_vh_esm1v_logreg.pkl
!experiments/checkpoints/esm1v/logreg/boughter_vh_esm1v_logreg.npz
!experiments/checkpoints/esm1v/logreg/boughter_vh_esm1v_logreg_config.json
!experiments/checkpoints/esm2_650m/logreg/boughter_vh_esm2_650m_logreg.pkl
!experiments/checkpoints/esm2_650m/logreg/boughter_vh_esm2_650m_logreg.npz
!experiments/checkpoints/esm2_650m/logreg/boughter_vh_esm2_650m_logreg_config.json

# Benchmark test logs (auto-generated)
experiments/benchmarks/logs/
experiments/benchmarks/*.log

# Large benchmark artifacts (regenerable, keep confusion matrices only)
experiments/benchmarks/**/predictions_*.csv
experiments/benchmarks/**/detailed_results_*.yaml
experiments/benchmarks/predictions_*.csv
experiments/benchmarks/detailed_results_*.yaml
test.log
training.log
